version: "3"
services:
  server:
    image: elyspio/electron-auto-update
    ports:
      - "4003:4000"
    environment:
      APPLICATIONS_LOCATION: /app/bin
    volumes:
      - ./bin/:/app/bin
    restart: always
    container_name: electron-auto-update
